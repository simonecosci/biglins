server {
    listen ${HTTP_PORT};
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host${HTTPS_REDIRECT_PORT_SUFFIX}$request_uri;
    }
}
